---
phase: 05-sidebar-and-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified: []
autonomous: false
requirements:
  - SIDE-01
  - SIDE-02
  - SIDE-03

must_haves:
  truths:
    - "Zero remaining references to sidebarOpen, setSidebarOpen, Menu icon, or X icon in page.tsx"
    - "The sidebar displays icons centered above small text labels at ~80px width"
    - "The active sidebar item has an amber/orange gradient background and amber left-edge indicator bar"
    - "The logo orb is circular with a visible breathing glow animation"
    - "The background image is visible through glass panels with appropriate darken overlay"
    - "All dashboard views (Overview, Clients, Meetings, Tasks) remain fully functional"
  artifacts: []
  key_links: []
---

<objective>
Verify Phase 5 implementation is complete by running automated grep checks and confirming all views function correctly with the new sidebar and background.

Purpose: Ensure no dead code remains from the old sidebar toggle pattern, confirm the warm sidebar and background image work correctly across all views, and gate phase completion on user visual approval.

Output: Grep verification results and user approval of complete Phase 5 implementation.
</objective>

<execution_context>
@/Users/robjames/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robjames/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-sidebar-and-polish/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated cleanup verification</name>
  <files></files>
  <action>
Run these automated checks and report results:

1. **Dead code check** — verify the old sidebar toggle pattern is fully removed:
   ```bash
   grep -n "sidebarOpen\|setSidebarOpen" src/app/page.tsx
   ```
   Expected: zero results

2. **Import cleanup check** — verify Menu and X icons are removed from imports:
   ```bash
   grep -n "Menu\|X," src/app/page.tsx | head -5
   ```
   Expected: zero results (note: `X` may appear in other contexts like "text-xl" — check only in import lines)

3. **Logo orb class check** — verify logo-orb is applied:
   ```bash
   grep -n "logo-orb" src/app/page.tsx
   ```
   Expected: 1 result

4. **Sidebar width check** — verify sidebar is fixed w-20:
   ```bash
   grep -n "w-20" src/app/page.tsx
   ```
   Expected: at least 1 result (the aside element)

5. **Icon-above-label layout check** — verify flex-col items-center on nav buttons:
   ```bash
   grep -n "flex-col items-center" src/app/page.tsx
   ```
   Expected: at least 1 result

6. **Background image CSS check** — verify bg-sunset reference in globals.css:
   ```bash
   grep -n "bg-sunset" src/app/globals.css
   ```
   Expected: 1 result

7. **Build compilation check:**
   ```bash
   npx next build 2>&1 | tail -10
   ```
   Expected: builds successfully

If any check fails, fix the issue before proceeding to the visual checkpoint.
  </action>
  <verify>
    <automated>cd "/Users/robjames/Projects/Mission Control/mission-control" && echo "=== sidebarOpen ===" && grep -c "sidebarOpen" src/app/page.tsx && echo "=== logo-orb ===" && grep -c "logo-orb" src/app/page.tsx && echo "=== w-20 ===" && grep -c "w-20" src/app/page.tsx && echo "=== flex-col ===" && grep -c "flex-col items-center" src/app/page.tsx && echo "=== bg-sunset ===" && grep -c "bg-sunset" src/app/globals.css</automated>
    <manual>sidebarOpen=0, logo-orb>=1, w-20>=1, flex-col>=1, bg-sunset>=1</manual>
  </verify>
  <done>All grep checks pass: no dead sidebar toggle code, logo-orb class applied, sidebar width fixed, icon-above-label layout in place, background image CSS present. Build compiles cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final visual verification of Phase 5</name>
  <files></files>
  <action>
This is a human-verify checkpoint for complete Phase 5 visual approval.

Complete Phase 5 sidebar restructure: slim fixed-width sidebar with icon-above-label navigation, amber/orange active states with indicator bar, circular logo orb with breathing pulse glow, and sunset ocean background image behind glassmorphism panels with darken overlay.

Verification steps:
1. Start the dev server if not running: `npm run dev`
2. Visit http://localhost:3000

**Sidebar checks:**
3. The sidebar is slim (~80px) — not wide (w-64)
4. Each nav item shows an icon centered above a small text label
5. All four labels readable: "Overview", "Clients", "Meetings", "Tasks"
6. No "Mission Control" text visible in the sidebar
7. No collapse/expand toggle button at the bottom of the sidebar

**Active state checks:**
8. Click each nav item — the active item shows amber/orange gradient background fill, left-edge amber-to-orange gradient vertical bar, and amber-colored text/icon
9. Hover over inactive items — they show a faint amber tint

**Logo checks:**
10. The logo at the top of the sidebar is a circle (not a rounded square)
11. The logo has a visible slow breathing glow animation (amber pulse)
12. No text label below the logo orb

**Background checks:**
13. The sunset ocean image is visible behind the glass panels
14. The image has a darken overlay making glass panel text readable
15. Ambient orbs (warm gold/rose/copper glows) still float visibly on top of the image
16. Glass panels have a frosted look against the background scene

**Functional checks:**
17. Click through all four views (Overview, Clients, Meetings, Tasks) — each renders correctly
18. No layout shifts or broken styling in any view
  </action>
  <verify>User visually confirms all 18 verification points above and types "approved"</verify>
  <done>Phase 5 complete — sidebar restructured, logo orb pulsing, background image visible, all views functional, user approved</done>
</task>

</tasks>

<verification>
- All automated grep checks pass (dead code removed, new classes applied)
- Build compiles successfully
- User visually approves sidebar layout, active states, logo orb, and background image
- All four dashboard views remain fully functional
</verification>

<success_criteria>
1. Zero dead code from old sidebar toggle pattern (sidebarOpen, Menu, X)
2. Build compiles with no errors
3. User confirms sidebar slim layout with icon-above-label format
4. User confirms active state amber indicator bar and gradient background
5. User confirms logo orb is circular with breathing pulse glow
6. User confirms background image visible through glass panels
7. User confirms all four views render correctly
</success_criteria>

<output>
After completion, create `.planning/phases/05-sidebar-and-polish/05-02-SUMMARY.md`
</output>
