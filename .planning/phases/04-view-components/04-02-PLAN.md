---
phase: 04-view-components
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: []
autonomous: false
requirements: [COMP-01, COMP-02, COMP-03, COMP-04]

must_haves:
  truths:
    - "Switching to Clients view shows fully warm amber/gold palette — no cyan/purple/pink visible in list or detail panel"
    - "Switching to Meetings view shows fully warm palette — AI badges orange, CTA buttons amber, spinners amber"
    - "Switching to Tasks view shows fully warm palette — priority badges warm, checkboxes amber hover, task list dividers amber"
    - "At-risk status indicators still read as danger (red-to-orange gradient, not full amber)"
    - "Active status indicators still read as healthy (green preserved unchanged)"
  artifacts: []
  key_links: []
---

<objective>
Visually verify the warm color conversion across all three view components in the running application — confirm no cool-color flashes on hover, focus, or interaction, and confirm semantic status colors remain distinct.

Purpose: Human eyes catch visual regressions that grep cannot — hover state flashes, contrast issues, subtle color mismatches between components. This checkpoint gates the phase as complete.

Output: User approval confirming all three views render fully warm with preserved semantic clarity.
</objective>

<execution_context>
@/Users/robjames/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robjames/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-view-components/04-CONTEXT.md
@.planning/phases/04-view-components/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run cross-file grep verification and build check</name>
  <files></files>
  <action>
Run the following automated checks before presenting for visual verification:

1. Cross-file grep — confirm zero cool-color residuals:
```bash
grep -rn "cyan\|purple\|pink\|indigo" src/app/components/ClientCommandCenter.tsx src/app/components/MeetingIntelligence.tsx src/app/components/TaskMissionControl.tsx
```
Expected: zero results.

2. Blue check on TaskMissionControl (P3 case):
```bash
grep -n "blue-500" src/app/components/TaskMissionControl.tsx
```
Expected: zero results.

3. Semantic color preservation check:
```bash
grep -n "emerald-500\|bg-red-500\b" src/app/components/ClientCommandCenter.tsx
```
Expected: emerald and red lines present (confirming getStatusColor/getHealthColor preserved).

4. Build compilation check:
```bash
cd "/Users/robjames/Projects/Mission Control/mission-control" && npx next build 2>&1 | tail -10
```
Expected: successful build with no type errors.

If any check fails, fix the issue before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd "/Users/robjames/Projects/Mission Control/mission-control" && grep -rn "cyan\|purple\|pink\|indigo" src/app/components/ClientCommandCenter.tsx src/app/components/MeetingIntelligence.tsx src/app/components/TaskMissionControl.tsx | wc -l | tr -d ' '</automated>
    <manual>Output must be "0"</manual>
  </verify>
  <done>All automated checks pass: zero cool-color residuals, blue-500 absent, semantic colors preserved, build compiles cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all three warm view components</name>
  <files></files>
  <action>
Present visual verification checkpoint to user. All three view components (ClientCommandCenter, MeetingIntelligence, TaskMissionControl) have been converted from cool neon to warm amber/gold/orange palette. CTA buttons use amber gradients, icon containers use amber-to-orange, secondary/AI elements use orange-400, structural dividers use amber-500/10, hover states are amber-400. Status indicators (active=green, at-risk=red-orange, prospective=yellow-amber) preserved for semantic clarity.

How to verify:
1. Start dev server if not running: `npm run dev`
2. Open http://localhost:3000 in browser

**Clients view (ClientCommandCenter):**
3. Click "Clients" in sidebar — verify the view renders with warm amber/gold colors
4. Check stat cards at top — icon containers should show amber-to-orange gradient backgrounds
5. Hover over a client card — name text should glow amber-400, card border should warm
6. Check the CTA button — should show amber gradient (from-amber-500 to-amber-600)
7. Click a client to open detail panel — verify modal header icon is amber-to-orange gradient
8. Check dividers in the detail panel — should be subtle warm amber, not cool indigo
9. Verify at-risk clients show red-to-orange avatar gradient (NOT full amber — red must be visible for danger)
10. Verify active clients show green avatar gradient (unchanged)

**Meetings view (MeetingIntelligence):**
11. Click "Meetings" in sidebar — verify warm rendering
12. Check AI-related elements — should use orange-400 text (secondary accent, distinct from amber primary)
13. Hover over meeting titles — should glow amber-400
14. Click "View" on a meeting — verify detail panel uses warm colors throughout
15. Check key insights bullet dots — should show amber-to-orange gradient dots
16. Verify loading spinner (if visible) is amber-400

**Tasks view (TaskMissionControl):**
17. Click "Tasks" in sidebar — verify warm rendering
18. Check priority badges — P1 should show red (semantic), P3 should show lighter amber
19. Hover over task checkboxes — border and background should warm to amber
20. Check task list dividers — subtle amber tint
21. Verify "day" project type label uses orange-400 text

**Cross-component consistency:**
22. Switch between all three views rapidly — verify consistent warm palette across all, no cool-color flashes on transition
23. Check that the two-tone system is visible: amber for primary/data elements, orange for AI/time/secondary elements

Resume signal: Type "approved" if all views render warm with preserved semantic status colors, or describe any issues found.
  </action>
  <verify>
    <automated>echo "CHECKPOINT — requires human visual approval"</automated>
    <manual>User approves all three views render fully warm with semantic status colors preserved</manual>
  </verify>
  <done>User approves visual verification of all three warm view components. Phase 4 complete.</done>
</task>

</tasks>

<verification>
Human visual approval confirms:
- All three view components fully warm
- No cool-color flashes on hover/focus/interaction
- Status indicators semantically distinct (traffic light: green/yellow-amber/red-orange)
- Two-tone accent system cohesive (amber primary + orange secondary)
</verification>

<success_criteria>
1. User types "approved" after visual inspection of all three views
2. Zero cool-color grep residuals confirmed by automated check
3. Build compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-view-components/04-02-SUMMARY.md`
</output>
