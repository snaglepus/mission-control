---
phase: 06-fix-incomplete-design-changes-header-and-nav
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/components/ClientCommandCenter.tsx
  - src/app/components/MeetingIntelligence.tsx
  - src/app/components/TaskMissionControl.tsx
autonomous: false
requirements:
  - MOBILE-05

must_haves:
  truths:
    - "ClientCommandCenter stats grid shows 2 columns on mobile with tighter padding"
    - "ClientCommandCenter filter bar wraps on narrow screens instead of overflowing"
    - "ClientCommandCenter client cards stack to single column with reduced padding on mobile"
    - "MeetingIntelligence stats grid shows 2 columns on mobile"
    - "MeetingIntelligence meeting list items stack content and buttons vertically on mobile"
    - "MeetingIntelligence filter bar wraps on narrow screens"
    - "TaskMissionControl stats grid shows 2 columns on mobile"
    - "TaskMissionControl workload grid reduces padding/text on mobile"
    - "TaskMissionControl filter bar wraps on narrow screens"
    - "All three view component headings reduce to text-2xl on mobile"
  artifacts:
    - path: "src/app/components/ClientCommandCenter.tsx"
      provides: "Mobile-responsive client view"
    - path: "src/app/components/MeetingIntelligence.tsx"
      provides: "Mobile-responsive meetings view"
    - path: "src/app/components/TaskMissionControl.tsx"
      provides: "Mobile-responsive tasks view"
  key_links:
    - from: "all three view components"
      to: "Tailwind sm: prefix"
      via: "responsive class variants"
      pattern: "sm:"
---

<objective>
Make all three view components (ClientCommandCenter, MeetingIntelligence, TaskMissionControl) responsive for mobile devices below 640px.

Purpose: While Plan 01 handles the page-level layout (sidebar, header, dashboard overview), the three view components still have desktop-only grids, filter bars, and content layouts that overflow or look cramped on narrow screens. This plan adds Tailwind responsive prefixes throughout all three components.

Output: All three view components display correctly at 390px width with 2-column stat grids, wrapping filter bars, stacked content items, and reduced padding/font sizes.
</objective>

<execution_context>
@/Users/robjames/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robjames/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-fix-incomplete-design-changes-header-and-nav/06-CONTEXT.md
@.planning/phases/06-fix-incomplete-design-changes-header-and-nav/06-RESEARCH.md
@src/app/components/ClientCommandCenter.tsx
@src/app/components/MeetingIntelligence.tsx
@src/app/components/TaskMissionControl.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make ClientCommandCenter responsive for mobile</name>
  <files>src/app/components/ClientCommandCenter.tsx</files>
  <action>
Apply responsive Tailwind class changes throughout ClientCommandCenter.tsx:

**Component heading:**
- Change `text-3xl` to `text-2xl sm:text-3xl` on the h1 element.
- Change the header layout from `flex items-center justify-between` to `flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4` so the "Add Client" button wraps below on mobile.
- Change "Add Client" button padding from `px-6 py-3` to `px-4 py-2.5 sm:px-6 sm:py-3` and text to include `text-sm sm:text-base`.

**Summary stats grid:**
- Change from `grid grid-cols-1 md:grid-cols-4 gap-6` to `grid grid-cols-2 md:grid-cols-4 gap-4 sm:gap-6`.
- Change each stat card's `p-6` to `p-4 sm:p-6`.
- Change stat icon containers from `w-12 h-12` to `w-10 h-10 sm:w-12 sm:h-12`.
- Change stat icons from `w-6 h-6` to `w-5 h-5 sm:w-6 sm:h-6`.
- Change stat values from `text-3xl` to `text-2xl sm:text-3xl`.

**Filter bar:**
- The filter bar already uses `flex flex-wrap items-center gap-4` which is good for mobile. Keep as-is.
- Change the search input container's `flex-1 max-w-md` to `flex-1 min-w-0` on mobile so it doesn't force overflow.

**Client grid:**
- The grid already uses `grid-cols-1 lg:grid-cols-2` which stacks on mobile. Keep column structure.
- Change client card `p-6` to `p-4 sm:p-6`.
- Inside each client card, change the inner revenue/health `grid grid-cols-2 gap-6` to `grid grid-cols-1 sm:grid-cols-2 gap-4 sm:gap-6` so on very narrow screens the revenue and health stack vertically.
- Change client name `text-xl` to `text-lg sm:text-xl`.
- Change the status badge and more button area: wrap the `flex items-center space-x-3` containing status and more button to handle narrow screens. Change the outer `flex items-start justify-between` to `flex flex-col sm:flex-row sm:items-start sm:justify-between gap-3` for the card header section.

**Client detail modal:**
- Change the modal's inner grid from `grid-cols-3` to `grid-cols-1 sm:grid-cols-3` so stats stack on mobile.
- Change modal max width to include responsive padding: keep `max-w-2xl w-full` but the outer `p-4` already handles screen edge padding.
- Change inner `p-6` sections to `p-4 sm:p-6`.

**Section spacing:**
- Change outer `space-y-8` to `space-y-6 sm:space-y-8`.
  </action>
  <verify>
    <automated>cd "/Users/robjames/Projects/Mission Control/mission-control" && npx next build 2>&1 | tail -5</automated>
    <manual>At 390px width: client stats show 2 columns, cards stack, filter wraps, modal fits screen</manual>
  </verify>
  <done>ClientCommandCenter is mobile-responsive — 2-column stats, stacking client cards, wrapping filters, responsive modal. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Make MeetingIntelligence and TaskMissionControl responsive for mobile</name>
  <files>src/app/components/MeetingIntelligence.tsx, src/app/components/TaskMissionControl.tsx</files>
  <action>
**MeetingIntelligence.tsx:**

Component heading:
- Change `text-3xl` to `text-2xl sm:text-3xl` on both h1 elements (main view and error view).

Stats grid:
- Change `grid grid-cols-1 md:grid-cols-4 gap-6` to `grid grid-cols-2 md:grid-cols-4 gap-4 sm:gap-6`.
- Change each stat card's `p-6` to `p-4 sm:p-6`.
- Change stat icon containers from `w-12 h-12` to `w-10 h-10 sm:w-12 sm:h-12`.
- Change stat icons from `w-6 h-6` to `w-5 h-5 sm:w-6 sm:h-6`.
- Change stat values from `text-3xl` to `text-2xl sm:text-3xl`.

Filter bar:
- Change `flex items-center space-x-4` to `flex flex-wrap items-center gap-3` (replace space-x-4 with gap-3 for wrapping).
- Change search input `flex-1 max-w-md` to `flex-1 min-w-0`.

Meeting list items:
- Change each meeting card's `p-6` to `p-4 sm:p-6`.
- Change the meeting item outer layout from `flex items-start justify-between` to `flex flex-col sm:flex-row sm:items-start sm:justify-between gap-3` so buttons wrap below content on mobile.
- Change meeting icon from `w-14 h-14` to `w-12 h-12 sm:w-14 sm:h-14`.
- Change meeting icon inner from `w-7 h-7` to `w-6 h-6 sm:w-7 sm:h-7`.
- Change meeting title from `text-lg` to `text-base sm:text-lg`.
- Change the metadata row `flex items-center space-x-6` to `flex flex-wrap items-center gap-x-4 gap-y-1` so date/duration/attendees wrap on narrow screens.
- Move the "View" button and "transcript" link section so it appears after the content on mobile (natural flow from the flex-col stacking).

Meeting detail modal:
- Change inner `p-6` sections to `p-4 sm:p-6`.
- The meta row already uses `flex flex-wrap items-center gap-6` — change `gap-6` to `gap-3 sm:gap-6`.
- Change modal heading `text-2xl` to `text-xl sm:text-2xl`.

Section spacing:
- Change outer `space-y-8` to `space-y-6 sm:space-y-8`.
- Change empty state `p-12` to `p-8 sm:p-12`.

---

**TaskMissionControl.tsx:**

Component heading:
- Change `text-3xl` to `text-2xl sm:text-3xl` on both h1 elements (main view and error view).
- Change the header action buttons area: the `flex items-center space-x-4` containing refresh + "Add Task" button — change to `flex items-center space-x-2 sm:space-x-4`.
- Change "Add Task" button from `px-6 py-3` to `px-4 py-2.5 sm:px-6 sm:py-3` and add `text-sm sm:text-base`.

Stats grid:
- Change `grid grid-cols-1 md:grid-cols-4 gap-6` to `grid grid-cols-2 md:grid-cols-4 gap-4 sm:gap-6`.
- Change each stat card's `p-6` to `p-4 sm:p-6`.
- Change stat icon containers from `w-12 h-12` to `w-10 h-10 sm:w-12 sm:h-12`.
- Change stat icons from `w-6 h-6` to `w-5 h-5 sm:w-6 sm:h-6`.
- Change stat values from `text-3xl` to `text-2xl sm:text-3xl`.

Workload overview card:
- Change outer `p-6` to `p-4 sm:p-6`.
- Change inner `grid grid-cols-3 gap-6` to `grid grid-cols-3 gap-3 sm:gap-6`.
- Change workload card inner `p-5` to `p-3 sm:p-5`.
- Change workload card icon `w-12 h-12` to `w-10 h-10 sm:w-12 sm:h-12` and inner icon `w-6 h-6` to `w-5 h-5 sm:w-6 sm:h-6`.
- Change workload card value `text-3xl` to `text-xl sm:text-3xl`.
- Change workload detail text `text-xs` remains fine, and `text-sm` stays.

Filter bar:
- The filter bar already uses `flex flex-wrap items-center gap-4`. Keep wrapping behavior.
- Change search input `flex-1 max-w-md` to `flex-1 min-w-0`.

Tasks list:
- Change task item padding `px-6 py-5` to `px-4 py-4 sm:px-6 sm:py-5`.
- Change task content text from `text-lg` to `text-base sm:text-lg`.
- Change the task metadata row `flex items-center space-x-4` to `flex flex-wrap items-center gap-2 sm:gap-4` so priority, project, date, and labels wrap on mobile.
- Change task list header `px-6 py-4` to `px-4 py-3 sm:px-6 sm:py-4`.

Completed tasks section:
- Change `px-6 py-4` to `px-4 py-3 sm:px-6 sm:py-4`.
- Change completed item `px-6 py-3` to `px-4 py-2 sm:px-6 sm:py-3`.

Section spacing:
- Change outer `space-y-8` to `space-y-6 sm:space-y-8`.
- Change empty state `py-12` to `py-8 sm:py-12`.
  </action>
  <verify>
    <automated>cd "/Users/robjames/Projects/Mission Control/mission-control" && npx next build 2>&1 | tail -5</automated>
    <manual>At 390px width: meeting stats 2-col, meeting items stack, task workload grid tighter, filters wrap</manual>
  </verify>
  <done>MeetingIntelligence and TaskMissionControl both mobile-responsive — 2-column stats, wrapping filters, stacked list items, tighter spacing. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of all view components on mobile</name>
  <files>src/app/components/ClientCommandCenter.tsx, src/app/components/MeetingIntelligence.tsx, src/app/components/TaskMissionControl.tsx</files>
  <action>
No code changes — this is a visual verification checkpoint.

What was built: Mobile-responsive versions of all three view components (ClientCommandCenter, MeetingIntelligence, TaskMissionControl). Stats grids show 2 columns, filter bars wrap, content items stack vertically, padding/fonts are reduced, and modals fit mobile screens.

How to verify:
1. Open the app in Chrome DevTools mobile simulation (iPhone 14, 390px)
2. Navigate to **Clients** view:
   - Stats show 2 columns, all 4 cards visible
   - Filter bar and search wrap naturally
   - Client cards display properly with stacked revenue/health on narrow screens
   - Click a client — modal fits the screen without horizontal overflow
3. Navigate to **Meetings** view:
   - Stats show 2 columns
   - Filter and search bar wrap
   - Meeting list items: icon + title + metadata stacked, buttons below content
   - Meeting metadata (date, duration, attendees) wraps instead of overflowing
   - Click a meeting — modal fits mobile screen
4. Navigate to **Tasks** view:
   - Stats show 2 columns
   - Workload grid shows 3 cards with tighter spacing (still readable)
   - Filter dropdowns and search wrap
   - Task items have tighter padding, metadata (priority, project, date, labels) wraps
5. Switch to desktop width (>640px) — verify all three views look unchanged from Phase 5

Resume signal: Type "approved" or describe issues to fix
  </action>
  <verify>User visual approval of all three view components at 390px width</verify>
  <done>User has approved the mobile-responsive Clients, Meetings, and Tasks views</done>
</task>

</tasks>

<verification>
- `npx next build` completes with zero errors
- All three view components display correctly at 390px width
- Stats grids show 2 columns, filter bars wrap, content stacks
- At desktop width, all views look identical to Phase 5 result
- No horizontal overflow or cut-off content on mobile
</verification>

<success_criteria>
- ClientCommandCenter: 2-column stats, wrapping filters, stacking cards at 390px
- MeetingIntelligence: 2-column stats, wrapping metadata, stacking meeting items at 390px
- TaskMissionControl: 2-column stats, tighter workload grid, wrapping task metadata at 390px
- All modals fit mobile viewport
- Desktop layout completely unchanged
- Build passes with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-fix-incomplete-design-changes-header-and-nav/06-02-SUMMARY.md`
</output>
