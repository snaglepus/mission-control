---
phase: 03-typography-page-frame
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/page.tsx
autonomous: false
requirements:
  - TYPO-01
  - TYPO-02
  - TYPO-03
  - TYPO-04

must_haves:
  truths:
    - "Gradient text headings display amber-to-gold-to-copper spectrum instead of cyan/purple"
    - "Stat values show a warm amber text glow on hover only, not at rest"
    - "All dividers and borders use amber tint instead of indigo tint"
    - "No cyan, purple, pink, or indigo color flashes appear on hover or focus anywhere in the page frame"
    - "grep -n 'cyan\\|purple\\|pink\\|indigo' src/app/page.tsx returns zero results"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Warm utility classes: .gradient-text, .neon-text, .neon-border, .sidebar-item, .glow-cyan (in-place warm), .text-glow (hover-only)"
      contains: "linear-gradient(90deg, #fcd34d"
    - path: "src/app/page.tsx"
      provides: "All inline Tailwind classes using warm amber/gold/orange palette"
      contains: "border-amber-500"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/app/globals.css"
      via: "className references to .gradient-text, .neon-text, .glow-cyan, .text-glow"
      pattern: "gradient-text|neon-text|glow-cyan|text-glow"
    - from: "src/app/page.tsx"
      to: "src/app/globals.css"
      via: ".glass-card:hover .text-glow rule enables hover-only stat glow"
      pattern: "glass-card:hover .text-glow"
---

<objective>
Replace all cool-color CSS utility classes in globals.css and all inline cool-color Tailwind classes in page.tsx with warm amber/gold/copper equivalents. The persistent header, sidebar, and dashboard overview become fully warm.

Purpose: Phase 3 completes the typography and page frame warm conversion -- after this, the entire persistent UI shell (sidebar, header, dashboard overview) renders in the warm amber palette with no remaining cool-color artifacts.

Output: Two modified files (globals.css, page.tsx) with zero cyan/purple/pink/indigo references.
</objective>

<execution_context>
@/Users/robjames/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robjames/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-typography-page-frame/03-CONTEXT.md
@.planning/phases/03-typography-page-frame/03-RESEARCH.md
@src/app/globals.css
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite globals.css utility classes from cool to warm</name>
  <files>src/app/globals.css</files>
  <action>
Rewrite these CSS utility classes in `src/app/globals.css` from cool neon to warm amber. Keep all structural CSS (position, sizing, animation, mask) unchanged -- only modify color values and add the hover-only glow behavior.

**`.gradient-text` (in `@layer components`):**
Replace `linear-gradient(135deg, #06b6d4 0%, #8b5cf6 100%)` with `linear-gradient(90deg, #fcd34d 0%, #f59e0b 50%, #ea580c 100%)` (amber-300 -> amber-500 -> orange-600, left-to-right per locked decision). Add `filter: drop-shadow(0 0 8px rgba(251, 191, 36, 0.25))` for the faint ambient glow. Do NOT use text-shadow (invisible with -webkit-text-fill-color: transparent).

**`.neon-text` (in `@layer components`):**
Replace `linear-gradient(135deg, #06b6d4 0%, #8b5cf6 50%, #ec4899 100%)` with `linear-gradient(90deg, #fcd34d 0%, #f59e0b 50%, #ea580c 100%)`. Same warm spectrum as .gradient-text -- one consistent gradient across all gradient headings per locked decision.

**`.neon-border::before` (in `@layer components`):**
Replace `linear-gradient(135deg, #06b6d4, #8b5cf6, #ec4899)` with `linear-gradient(135deg, #fcd34d, #f59e0b, #ea580c)`. Keep 135deg direction for the border effect (different from text which is 90deg).

**`.sidebar-item::before` (in `@layer components`):**
Replace `linear-gradient(180deg, #06b6d4, #8b5cf6)` with `linear-gradient(180deg, #f59e0b, #ea580c)`. Vertical warm gradient for the active indicator bar.

**`.glow-cyan` (in `@layer components`):**
Update the box-shadow value IN-PLACE (keep the class name `.glow-cyan` to avoid JSX reference mismatches): change `rgba(6, 182, 212, 0.3)` to `rgba(251, 191, 36, 0.3)`. This is the safer migration approach per research -- avoids disconnected renames.

**`.glow-purple` (in `@layer components`):**
Update in-place similarly: change `rgba(139, 92, 246, 0.3)` to `rgba(234, 88, 12, 0.3)` (orange-600 glow). This class is still referenced in the codebase and may be used by Phase 4 components, so keep it warm rather than deleting.

**`.text-glow` (in `@layer utilities`):**
Move from `@layer utilities` to `@layer components` (needed for hover pseudo-class support). Rewrite to hover-only warm amber glow per locked decision:
```css
.text-glow {
  text-shadow: none;
  transition: text-shadow 150ms ease;
}

.glass-card:hover .text-glow {
  text-shadow: 0 0 8px rgba(251, 191, 36, 0.35);
}
```
The `@layer utilities` block should be removed entirely (it only contained `.text-glow`). The hover-only behavior matches locked decision: "Glow appears on hover only, not at rest."

**Do NOT modify:** `.glass-card`, `.hover-lift`, `.status-dot`, `.card-gradient`, `.ambient-orbs`, `.orb-*`, `@keyframes` -- these are owned by other phases or already warm.
  </action>
  <verify>
    <automated>cd /Users/robjames/Projects/Mission\ Control/mission-control && grep -n "06b6d4\|8b5cf6\|ec4899\|rgba(6, 182, 212\|rgba(139, 92, 246\|rgba(6,182,212" src/app/globals.css; echo "EXIT:$?"</automated>
    <manual>The grep should return zero lines (only the EXIT:1 for no matches). All cool hex/rgba values should be replaced with warm equivalents.</manual>
  </verify>
  <done>All 7 utility classes in globals.css use warm amber/gold/orange colors. .text-glow is hover-only with 150ms transition. No cool-color hex or rgba values remain in any utility class definition. The @layer utilities block is removed.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all inline cool-color Tailwind classes in page.tsx</name>
  <files>src/app/page.tsx</files>
  <action>
Replace every inline cool-color Tailwind class in `src/app/page.tsx` with warm amber/gold/orange equivalents. This is a comprehensive substitution covering sidebar, header, stats, quick actions, and recent activity. Use the "redesigned warmth" approach per locked decision -- pick warm colors by visual intent, not shade-for-shade mapping.

**Sidebar section (lines ~42-93):**
- Line 42: `bg-[#0f172a]/80` -> `bg-[#1a0f00]/80` AND `border-indigo-500/10` -> `border-amber-500/10`
- Line 45: `border-indigo-500/10` -> `border-amber-500/10`
- Line 46: `from-cyan-400 to-purple-600` -> `from-amber-400 to-orange-600` (logo orb gradient). Keep `glow-cyan` class name (its CSS is already warm from Task 1).
- Line 65 (active nav): `from-cyan-500/20 to-purple-600/20 text-cyan-400` -> `from-amber-500/20 to-orange-600/20 text-amber-400`
- Line 66 (inactive nav hover): `hover:text-slate-200` -> `hover:text-amber-300` and `hover:bg-white/5` -> `hover:bg-amber-500/5` (warm hover per locked decision)
- Line 70 (active icon bg): `bg-cyan-500/20` -> `bg-amber-500/20`
- Line 72 (active icon text): `text-cyan-400` -> `text-amber-400`
- Line 78 (active dot): `bg-cyan-400 glow-cyan` -> `bg-amber-400 glow-cyan` (class name stays, CSS is warm)
- Line 86: `border-indigo-500/10` -> `border-amber-500/10`

**Header section (lines ~99-128):**
- Line 99: `bg-[#0f172a]/60` -> `bg-[#1a0f00]/60` AND `border-indigo-500/10` -> `border-amber-500/10`
- Line 106 (search input): `border-indigo-500/20` -> `border-amber-500/20`, `focus:border-cyan-500/50` -> `focus:border-amber-500/50`, `focus:ring-cyan-500/20` -> `focus:ring-amber-500/20`. Also change `bg-slate-900/50` -> `bg-[#1a0f00]/50` (warm dark background instead of cool slate).
- Line 113 (notification dot): `from-pink-500 to-purple-500` -> `from-amber-400 to-orange-500` (two warm tones per locked decision to preserve multi-tone)
- Line 118: `border-indigo-500/20` -> `border-amber-500/20`
- Line 123 (avatar gradient): `from-cyan-500 to-purple-600` -> `from-amber-500 to-orange-600`. Keep `glow-cyan` class name.

**DashboardOverview -- Stats data (lines ~144-177):**
- Stats[0] (Active Clients): `gradient: "from-cyan-500 to-cyan-600"` -> `"from-amber-400 to-amber-600"`, `glow: "shadow-cyan-500/30"` -> `"shadow-amber-500/30"`
- Stats[1] (Weekly Revenue): `gradient: "from-purple-500 to-pink-500"` -> `"from-orange-400 to-orange-600"`, `glow: "shadow-purple-500/30"` -> `"shadow-orange-500/30"`
- Stats[2] (Meetings): `gradient: "from-emerald-500 to-teal-500"` -> `"from-yellow-400 to-amber-500"` (convert to warm per wide spectrum decision), `glow: "shadow-emerald-500/30"` -> `"shadow-yellow-500/30"`
- Stats[3] (Olivia Tasks): already warm (`amber-500 to-orange-500`) -- no change needed.

Also add `text-glow` class to the stat value `<p>` elements so they get the hover-only warm glow from Task 1. Change the stat value `<p>` tag (line ~228) from `className="text-3xl font-bold text-white mb-2"` to `className="text-3xl font-bold text-white mb-2 text-glow"`.

**DashboardOverview -- Quick Actions (lines ~179-267):**
- Line 185: `color: "cyan"` -> `color: "amber"`
- Line 192: `color: "purple"` -> `color: "orange"`
- Line 244-248 colorMap: Replace entire colorMap:
  ```typescript
  const colorMap: Record<string, string> = {
    amber: "from-amber-400 to-amber-600 shadow-amber-500/30",
    orange: "from-orange-400 to-orange-600 shadow-orange-500/30",
    emerald: "from-emerald-500 to-emerald-600 shadow-emerald-500/30",
  };
  ```
- Line 260: `text-cyan-400` -> `text-amber-400`

**DashboardOverview -- Recent Activity (lines ~270-310):**
- Line 274: `text-cyan-400` (Clock icon) -> `text-amber-400`
- Line 277: `text-cyan-400 hover:text-cyan-300` -> `text-amber-400 hover:text-amber-300`
- Line 283: `color: "cyan"` -> `color: "amber"`
- Line 285: `color: "purple"` -> `color: "orange"`
- Line 288: `border-indigo-500/10` -> `border-amber-500/10`
- Lines 290-293: Update the color conditional:
  `item.color === "cyan" ? "bg-cyan-400"` -> `item.color === "amber" ? "bg-amber-400"`
  `item.color === "purple" ? "bg-purple-400"` -> `item.color === "orange" ? "bg-orange-400"`
- Lines 300-302: Update badge conditionals:
  `"bg-cyan-500/20 text-cyan-400"` -> `"bg-amber-500/20 text-amber-400"`
  `"bg-purple-500/20 text-purple-400"` -> `"bg-orange-500/20 text-orange-400"`

**Final check:** After all substitutions, `grep -n "cyan\|purple\|pink\|indigo" src/app/page.tsx` must return zero results. Also `grep -n "0f172a" src/app/page.tsx` must return zero results (all cool navy hex replaced with warm `#1a0f00`).
  </action>
  <verify>
    <automated>cd /Users/robjames/Projects/Mission\ Control/mission-control && echo "=== Cool color check ===" && grep -cn "cyan\|purple\|pink\|indigo" src/app/page.tsx && echo "=== Cool hex check ===" && grep -cn "0f172a" src/app/page.tsx; echo "EXIT:$?"</automated>
    <manual>Both grep counts should be 0. No cyan, purple, pink, indigo named colors and no #0f172a hex values remain in page.tsx.</manual>
  </verify>
  <done>All ~31 lines with cool-color Tailwind classes in page.tsx are replaced with warm amber/gold/orange equivalents. Data-driven color strings (stats, quickActions, activity feed) use "amber" and "orange" keys. Hardcoded bg-[#0f172a] replaced with bg-[#1a0f00]. The grep success criterion returns zero matches for cyan, purple, pink, indigo, and 0f172a.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of warm typography and page frame</name>
  <files>src/app/globals.css, src/app/page.tsx</files>
  <action>
Human visual verification of the warm typography and page frame conversion. No automated work in this task -- all implementation is complete from Tasks 1 and 2.

What was built: Complete warm conversion of the page frame -- gradient text headings now show amber-to-gold-to-copper spectrum, all sidebar/header borders use amber tint, stat values glow warm amber on hover, all dividers are warm, and no cool color flashes appear during interaction.

How to verify:
1. Open http://localhost:3000 in the browser
2. Check the "Welcome back, Robbie" heading -- gradient text should show warm amber -> gold -> copper with a faint ambient glow (golden hour feel, not metallic)
3. Check the "Mission Control" logo text in the sidebar -- same warm gradient
4. Check sidebar nav: active item should have amber tint background and amber text, indicator dot should be amber
5. Check sidebar/header borders: should be subtle amber tint, not indigo
6. Hover over a stat card: the stat number should gain a subtle warm amber text glow (~2px spread, barely visible)
7. Check stats icon badges: first two should be amber/orange tones, third (Meetings) should be yellow-to-amber
8. Click Quick Actions cards: icon badges should show amber/orange/emerald (no cyan/purple)
9. Check Recent Activity: dot colors, badges should use amber/orange (no cyan/purple)
10. Tab through interactive elements: no cyan/purple/pink flash should appear on focus
11. Check notification bell dot: should be amber-to-orange gradient (not pink-to-purple)
12. Check search input focus ring: should glow amber (not cyan)

Resume signal: Type "approved" to complete Phase 3, or describe any issues to address.
  </action>
  <verify>
    <automated>cd /Users/robjames/Projects/Mission\ Control/mission-control && echo "Cool colors in page.tsx:" && grep -c "cyan\|purple\|pink\|indigo" src/app/page.tsx || true && echo "Cool colors in globals.css:" && grep -c "06b6d4\|8b5cf6\|ec4899" src/app/globals.css || true</automated>
    <manual>Both counts should be 0. Visual inspection confirms warm amber palette throughout.</manual>
  </verify>
  <done>User has visually approved the warm typography and page frame. All gradient text, borders, dividers, stat glows, and interactive states render in the warm amber/gold/copper palette with no cool-color artifacts.</done>
</task>

</tasks>

<verification>
1. `grep -n "cyan\|purple\|pink\|indigo" src/app/page.tsx` returns zero results
2. `grep -n "0f172a" src/app/page.tsx` returns zero results
3. `grep -n "06b6d4\|8b5cf6\|ec4899" src/app/globals.css` returns zero results
4. `grep -n "rgba(6, 182, 212\|rgba(139, 92, 246" src/app/globals.css` returns zero results
5. Dev server compiles without errors (`npm run dev`)
6. Visual inspection confirms warm amber palette throughout the page frame
</verification>

<success_criteria>
- Gradient text renders warm amber -> gold -> copper spectrum with faint drop-shadow glow
- Stat values show hover-only warm amber text glow (no glow at rest)
- All dividers/borders use amber tint instead of indigo
- Zero cool-color artifacts visible during keyboard navigation or mouse hover
- grep checks pass with zero matches for cool colors in both files
- User visually approves the warm page frame
</success_criteria>

<output>
After completion, create `.planning/phases/03-typography-page-frame/03-01-SUMMARY.md`
</output>
